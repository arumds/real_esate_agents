openai>=1.30.0
mcp>=1.0.0
shap>=0.45.0        # for wiring in a real model's SHAP explainer
scikit-learn>=1.4.0 # or your real valuation/rental model's framework
google-adk>=2.8.0   # for real_estate_agents/ -- multi-agent orchestration
# google-adk[extensions] instead, if using OpenAI via LiteLLM (see real_estate_agents/agent.py)
langchain-core>=0.3.0          # for langchain_rag/ -- real RAG pipeline used by real_estate_agents/
langchain-text-splitters>=0.3.0
langchain-community>=0.3.0     # FAISS vectorstore wrapper
faiss-cpu>=1.8.0                # the actual vector index used by langchain_rag/retriever.py
pytest>=8.0.0       # for eval/test_deterministic.py
python-dotenv>=1.0.0 # loads .env for OPENAI_API_KEY/ADK_MODEL/GOOGLE_API_KEY -- main.py, both mcp_server.py, eval/run_golden_eval.py
